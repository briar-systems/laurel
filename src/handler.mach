# application handler contract

use std.types.result.res;
use laurel.context;
use laurel.error;

pub val RESPOND: u8 = 1;
pub val NEXT:    u8 = 2;

pub def HandlerFun: fun(ptr, *context.Context) res[u8, error.AppError];

pub rec Handler {
    ctx:  ptr;
    call: HandlerFun;
}
